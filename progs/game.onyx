
gfx_draw_rect :: foreign "gfx" "draw_rect" proc (x i32, y i32, w i32, h i32) ---

export main :: proc {
    update();
    draw();
}

update :: proc {}

draw :: proc {
    gfx_draw_rect(0, 0, 100, 100);
}

