/* This is a comment */

log :: proc (a i32, b i32) -> i32 ---;

export add :: proc (a i32, b i32) -> i32 {
	/* More comments */
	return a + b;
};

export mul :: proc (a i32, b i32) -> i32 {
	c: i32 = a - b;
	d := a + 2;
	return c * d;
};
