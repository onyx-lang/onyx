#load "core/module"

use core {*}

main :: (args: [] cstr) {

    S := Set.make(i32)

    Set.insert(&S, 5)
    Set.insert(&S, 5)
    Set.insert(&S, 6)
    for entry in S.entries do println(entry.value)

    println(Set.has(&S, 5))

    println("--------------")
    Set.remove(&S, 5)
    for entry in S.entries do println(entry.value)
    println(Set.has(&S, 5))
}