#load "scripts/c_library"
use package core

main :: () {
    path := module_path(#file);

    compile_c_file(
        string.concat(path, "stb_image.c"),
        string.concat(path, "stb_image.so"));
}