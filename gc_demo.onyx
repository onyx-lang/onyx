use core {*}
use runtime

main :: () {
    alloc.report_leaks_in_scope()

    use arr := Slice.copy(.[ 1, 2, 3, 4, 5 ])
    println(arr)

    use m := foo()
    println(m)
}

foo :: () -> Map(str, i32) {
    return Map.literal(str, i32, .[
        .{ "test", 1 },
        .{ "asdf", 2 }
    ])
}